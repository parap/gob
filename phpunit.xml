<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnWarning="true"
         failOnNotice="true">
    <testsuites>
        <testsuite name="Goblin">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <php>
        <!-- Repository tests build this schema from schema.sql and delete what
             they create. The name must end in _test; the base class refuses
             anything else so a suite can never chew through the dev database. -->
        <env name="DB_TEST_NAME" value="gob_test"/>
    </php>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
